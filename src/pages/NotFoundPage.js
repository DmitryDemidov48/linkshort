
import React from 'react';
import styled from 'styled-components';

// Создаем стилизованный компонент для контейнера страницы "Страница не найдена"
const NotFoundContainer = styled.div`
  max-width: 800px; // Максимальная ширина контейнера страницы
  margin: 0 auto; // Центрирование контейнера по горизонтали
  padding: 20px; // Отступы внутри контейнера
`;

// Создаем функциональный компонент для страницы "Страница не найдена"
const NotFoundPage = () => {
    return (
        // Рендерим стилизованный контейнер страницы "Страница не найдена"
        <NotFoundContainer>
            {/* Заголовок страницы */}
            <h2>Страница не найдена</h2>
            {/* Текст об ошибке */}
            <p>Запрашиваемая страница не найдена. Проверьте правильность URL-адреса и попробуйте снова.</p>
        </NotFoundContainer>
    );
};

// Экспортируем компонент NotFoundPage для использования в других частях приложения
export default NotFoundPage;
