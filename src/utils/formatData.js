// Функция для форматирования данных перед их отправкой на сервер
const formatData = (data) => {
    // Производим необходимые преобразования данных
    // Например, приводим данные к определенному формату или добавляем дополнительные поля
    const formattedData = {
        ...data, // Копируем все поля из исходных данных
        // Добавляем дополнительное поле или преобразовываем существующие данные
    };

    // Возвращаем отформатированные данные
    return formattedData;
};

// Экспортируем функцию для использования в других модулях
export default formatData;
